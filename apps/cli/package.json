{
  "name": "@orbit/cli",
  "version": "0.1.0",
  "description": "Switch coding conversations between Codex and Claude Code with Git-backed history",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "prepack": "npm run build --prefix ../..",
    "prepublishOnly": "npm run build --prefix ../.."
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "bin": {
    "orbit": "./bundle/orbit.js"
  },
  "files": [
    "bundle",
    "README.md"
  ],
  "engines": { "node": ">=22.14.0" },
  "publishConfig": { "access": "public" }
}
