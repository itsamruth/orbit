# @orbit/cli

Install globally and run Orbit inside a source repository:

```bash
npm install --global @orbit/cli
orbit init
orbit claude
orbit switch codex
```

See the repository README for setup, privacy, publishing, and development details.
